import { whatTime } from "./utils"

/**
 * hello ts commnet copyr ..test
 * @returns sdfs
 */
const helleTs = (): string => {
    const val: string = whatTime()
    return val
}

export default helleTs